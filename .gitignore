# Rust 构建输出
/target
Cargo.lock

# IDE 配置
**/.vscode
.cursor/

# AI agent 本地私有配置（Skills 等）
.agents/

# 二进制文件
**/*.bin

# 演化模型文件
**/*.otm

# 模型权重产物（PyTorch ckpt / ONNX / 等，由示例脚本生成，不入仓）
/models/

# 生成的训练数据
/data/

# 临时笔记/问题记录（本地使用）
/issues
__pycache__/
*.pyc
